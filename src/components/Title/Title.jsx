import React from "react";

const Title = ({ title }) => {
  return (
    <h2 className="text-center text-4xl font-semibold text-gray-800 lg:text-5xl">
      {title}
    </h2>
  );
};

export default Title;
